import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Hello, no menus")
            .font(.largeTitle)
            .padding()
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
